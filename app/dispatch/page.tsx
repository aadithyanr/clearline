import DispatchPageContent from './DispatchPageContent';

export const metadata = {
  title: 'Dispatch Dashboard - ERoute Clearline',
  description: 'Live emergency dispatch tracking dashboard.',
};

export default function DispatchPage() {
  return <DispatchPageContent />;
}
